import styled from "styled-components";

const ScaleInfoWrapper = styled.div`
    margin-bottom: 1rem;
    text-align: center;
	font-size: 2rem;
    font-weight: 700;
    margin-bottom: 40px;
    color: #333;
`;

const ScaleName = styled.span`
    margin-right: 1.5rem;
    color: #e74c3c;
`;

const ScaleNotes = styled.span`
    color: #555;
    font-weight: 400;
`;

export { ScaleInfoWrapper, ScaleName, ScaleNotes };
